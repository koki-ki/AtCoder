#include <bits/stdc++.h>
using namespace std;
using ll = long long;
#define rep(i, n) for (ll i = 0; i < (ll)n; i++)

int main() {
    vector<int> vec(3);
    cin >> vec.at(0) >> vec[1] >> vec.at(2);
    cout << vec[0] << endl;
}