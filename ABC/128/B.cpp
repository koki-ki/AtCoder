#include <bits/stdc++.h>
using namespace std;
using ll = long long;
#define rep(i, n) for (ll i = 0; i < (ll)n; i++)
#define rep2(i, s, n) for (ll i = s; i < (ll)n; i++)
using Graph = vector<vector<ll>>;
using pll = pair<ll, ll>

    int main() {
    ll n;
    cin >> n;
    pair<pair<string, ll>, ll> r[150];
    ll s, p;
    rep2(i, 0, n) {
        cin >> s >> p;
        r[i] =
    }
}
