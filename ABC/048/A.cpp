#include <bits/stdc++.h>
using namespace std;
using ll = long long;
#define rep(i, n) for (ll i = 0; i < (ll)n; i++)

int main() {
    string atcoder, s, contest;
    cin >> atcoder >> s >> contest;
    cout << "A" << s.at(0) << "C" << endl;
}